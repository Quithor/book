server:
  port: 8327
spring:
  application:
    name: book
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book?severTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false
    username:
    password:
logging:
  file:
    path:
  level:
    root: info
    top.yanquithor.student: debug